<div class="container">
  <div class="row">
    <%= simple_form_for(current_user) do |f| %>
        <% if current_user.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(current_user.errors.count, "error") %> prohibited this info from being saved:</h2>

              <ul>create
                <% current_user.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
              </ul>
            </div>
        <% end %>
        <div class="container">
          <div class="row">
            <%= f.input :first_name%>
            <%= f.input :last_name%>
            <%= f.input :nickname%>
            <br>

            <div class="actions">
              <%= f.submit "Submit", class: 'btn btn-info' %>
            </div>
          </div>
        </div>
        <br>
    <% end %>
  </div>
</div>
